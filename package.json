{
  "name": "opd-care-platform",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "web",
    "mobile"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build:shared": "npm run build -w shared",
    "build:server": "npm run build -w server",
    "build:web": "npm run build -w web",
    "build": "npm run build:shared && npm run build:server && npm run build:web",
    "typecheck:mobile": "npm run build:shared && npm run typecheck -w mobile",
    "test:server": "npm run build:shared && npm test -w server"
  }
}
